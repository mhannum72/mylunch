// session.js - retrieve a user's session.  I store the session information
// in the mongodb table, 'session'.  In the future, I'd like to store a number
// of these in an array in memory, and periodically age out the oldest victim,
// but since I would always have to resort to using a db lookup anyway, the
// initial version will do just that.  And hey, mongodb is supposed to be crazy
// fast, right?

Session = function(){};
Session.prototype.tables = {};


exports.initSession = function (intables) {
    tables = intables;
}

exports.retrieveSession = function (err, req, res, next) {
    if(err) {
        throw (err);
    }

    // Get the session cookie.
    tables.session_table.findOne();

}

